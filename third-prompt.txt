第四阶段
该阶段主要在于部分末尾功能补全和api接入
服务器终端显示系统
按键可配置
语言文件与language
language自适配
在客户端的配置文件中加一个语言锁
当语言锁上锁时 系统不能自动修改玩家的语言
当玩家进入世界时如果语言锁没有上锁则修改为玩家所使用的语言 如果需要切换语言在模组中没有对应的语言文件 则默认切到英语
配置文件初始化（初次生成）时语言锁默认不上锁
当玩家使用/areahint language指令后将语言锁上锁
配置文件设置可视化
我现在需要你将现在的客户端配置文件中的所有可配置选项可以用我的世界本身的设置打开并进行修改（可参考sodium的可视化配置 风格与原版相同）这个界面是可视化的 每个选项给出配置 修改后重新载入玩家客户端配置

可视化功能
现在我需要你为我写一个可视化功能 以下的提示词请你一字一句就行研读 不要错过任何一个字 
子指令 : `/areahint boundviz`
1.  名称：`boundviz`
2.  功能：该指令用于显示玩家所在维度的所有域名的边界
使用此指令时 像easyadd指令一样是交互式的 我现在要你把这个指令写为交互式的（参考easyadd指令）
输入该指令后 显示所有没有被方块遮挡的域名部分 每个域名需要显示的有域名高度最低的一级顶点之间连起来的线段与域名高度最高的一级顶点之间连起来的线段联合成的面与域名高度最低的一级顶点形成的面与域名高度最高的一级顶点形成的面 如果面被方块遮挡 被遮挡部分不显示 面与方块有接触的地方则在方块与面的交接处渲染上域名的边界线 域名的边界线与面是有颜色的边界线的不透明度为80% 面的不透明度为20% 面与顶点的颜色取决于该域名的颜色（域名颜色在玩家所处维度的域名文件中）注意显示的域名应该是玩家所处维度的域名 其他维度的域名不能加载
同时 在easyadd、expandarea和shrinkarea指令记录顶点时 记录的一级顶点应该实时更新显示并连成线 该线的不透明度为100%颜色为白色虚线 如果线被方块遮挡 被遮挡部分不显示 面与方块有接触的地方则在线在方块竖直投影在面的交接处渲染上域名的边界线
该指令第一次使用则开启第二次使用则关闭 玩家初始化时默认为关闭 这个开启和关闭应该写入配置文件 在初始化配置文件时，也应该加入这个选项

api接入
vulkan
现在原有切换渲染方式的命令的vulkan部分是没有实现的
现在Opengl的渲染代码是能够正常运行且效果较好的 我需要你参考OpenGL的代码把它实现为vulkan的渲染代码（注意是写在原有vulkan应当实现的位置）效果相同 逻辑相同
我现在需要你接入vulkan模组的api 当游戏启动时检查模组是否使用vulkan的mod如果是则修改用户配置文件为vulkan 如果不是若配置文件为vulkan则修改为opengl若配置文件为OpenGL或者CPU则不修改
注意vulkan模组不是必须的 如果没有vulkan模组不影响运行 如果使用指令切换渲染方式 不能从vulkan切换到OpenGL或者cpu 反之同理
bluemap
Luckperms
xaero's minimap
这是该模组的解包的文件 请你找出该模组提供的api和接口 并给出相应用法和说明

版本分支
这是一个我的世界JAVA版1.20.4fabric模组请你根据现在已有文件所调用的api与其的当前的调用方法以及功能实现方式 然后查询目标版本的api和api的调用方法以及思考功能实现的适配 将该模组适配到fabric的xxx

子指令 : `/areahint tmux`
1.  名称：`tmux`
2.  功能：使用此指令时 将玩家的聊天栏切换到私人模式 在该模式下玩家发出的所有信息不会被他人看到只能被自己或者服务端看到 再次使用该指令时切回公共模式（原来世界的聊天栏）注意：切回公共模式时应该展示出全部的聊天信息包括切回私人模式时可以回看之前的所有信息（回看部分的功能实现应当仅限于客户端） 它应该与原版一样当你退出世界的时候不再保留聊天栏的所有信息（这一部分参照原版的代码）
我要求该功能尽量不修改原版代码 至少在公共模式时与原版相同 该功能的存在目的是为了在该模组进行交互的时候不被他人看到所以说要与交互适配
目标选择器：作用于服务端和客户端
权限等级：0 (不需要管理员)
传送模块
子指令 : `/areahint tcp`
1.  名称：`tcp`
2.  功能：该指令有两种使用方法 直接使用此指令时 向玩家列出所有的域名 玩家选择其中一个域名 或者在指令后面跟随一个域名 确定后使用所保存的格式将玩家传送到指定域名的中心点的一个两格高且下方是方块的位置优先从高向低遍历如果中心点没有则从中心点向外遍历直到找到位置
目标选择器：作用于玩家自己
权限等级：0 (使用该玩家的权限)
子指令 : `/areahint udp`
1.  名称：`udp`
2.  功能：该指令有两种使用方法 直接使用此指令时 向玩家列出所有的域名 玩家选择其中一个域名 或者在指令后面跟随一个域名 确定后使用所保存的格式将玩家随机传送到指定域名的一个两格高且下方是方块的位置
目标选择器：作用于玩家自己
权限等级：0 (使用该玩家的权限)
签名扩展
让域名权限更自由
使用交互式指令：参考easyadd指令
子指令 : `/areahint addsignature`
1.  名称：`addsignatire`
2.  功能：使用此指令时 向玩家询问需要扩展签名的域名 输入域名后应检测该玩家是否为该域名的basename引用的玩家或者是管理员（权限等级为二） 如果是则列出所有可以被修改的域名（如果是普通玩家则为被basename引用的玩家的域名 如果玩家为管理员则列出所有域名）玩家选择域名后 询问扩展的玩家名字 确认后 模组将完整域名 发送给服务端 服务端需要接收客户端发来的域名，服务端会将其写入相对应的世界的维度的域名文件中，并将其重新分发给每一个玩家（相当于执行一次reload指令）
权限等级：0 (不需要管理员)
子指令 : `/areahint deletesignature`
1.  名称：`deletesignatire`
2.  功能：使用此指令时 向玩家询问需要删除签名的域名 输入域名后应检测该玩家是否为该域名的basename引用的玩家或者是管理员（权限等级为二） 如果是则列出所有可以被修改的域名（如果是普通玩家则为被basename引用的玩家的域名 如果玩家为管理员则列出所有域名）玩家选择域名后 询问删除的玩家名字（参考deletehint指令） 确认后 再次进行二次确认 模组将完整域名 发送给服务端 服务端需要接收客户端发来的域名，服务端会将其写入相对应的世界的维度的域名文件中，并将其重新分发给每一个玩家（相当于执行一次reload指令）
权限等级：0 (不需要管理员)
域名描述数据库
服务端在启动时检查对应的世界是否有database文件夹 如果没有则创建
该文件夹内为每个维度的维度文件夹 维度文件夹的每一个json文件的名字与域名联合名字相同（不是域名真实名字）
json文件储存的是该联合域名的描述
玩家（客户端）按下绑定的按键时 如果没有被其他功能占用（如easyadd指令 expandarea指令等等）则向服务端查询当前所处的域名的联合域名的描述（该查询优先级比指令占用更低） 服务端进行查询并反馈后 在客户端（玩家）视角中用原版的书籍展现得到的文本（这一部分尽量调用原版的实现）如果没有查询到文件则显示对应域名暂无描述
注意：
描述文件并非是域名所必要的 两者并非绑定关系
维度域名也有描述 直接储存于database下为json文件
这里所有指令应当像easyadd指令一样使用交互式界面
子指令 : `/areahint adddescription`
1.  名称：`adddescription`
2.  功能：使用此指令时 向玩家询问需要添加描述的域名 输入域名后应检测该玩家是否为该域名的basename引用的玩家或者是管理员（权限等级为二） 如果是则列出所有可以被修改的域名（如果是普通玩家则为被basename引用的玩家的域名 如果玩家为管理员则列出所有域名）玩家选择域名后 让玩家输入描述 输入后 模组将完整描述 发送给服务端 服务端需要接收客户端发来的描述，服务端会将其写入相对应的世界的对应维度的联合域名描述文件中
权限等级：0 (不需要管理员)
子指令 : `/areahint deletedescription`
1.  名称：`deletedescription`
2.  功能：使用此指令时 向玩家询问需要删除描述的域名 输入域名后应检测该玩家是否为该域名的basename引用的玩家或者是管理员（权限等级为二） 如果是则列出所有可以被修改的域名（如果是普通玩家则为被basename引用的玩家的域名 如果玩家为管理员则列出所有域名）玩家选择域名后 让玩家确认 确认后 进行二次确认 模组发送给服务端 服务端需要接收客户端发来的描述，服务端会将其删除相对应的世界的对应维度的联合域名描述文件
权限等级：0 (不需要管理员)
子指令 : `/areahint adddimensionalitydescription`
1.  名称：`adddimensionalitydescription`
2.  功能：使用此指令时 向玩家询问需要添加描述的维度域名 输入域名后应检测该玩家是否为管理员（权限等级为二） 如果是则列出所有可以被修改的维度域名 玩家选择域名后 让玩家输入描述 输入后 模组将完整描述 发送给服务端 服务端需要接收客户端发来的描述，服务端会将其写入相对应的世界的对应维度的联合域名描述文件中
权限等级：2 (管理员)
子指令 : `/areahint deletedimensionalitydescription`
1.  名称：`deletedimensionalitydescription`
2.  功能：使用此指令时 向玩家询问需要删除描述的域名 输入域名后应检测该玩家是否为管理员（权限等级为二） 如果是则列出所有可以被修改的维度域名 玩家选择域名后 让玩家确认 确认后 进行二次确认 模组发送给服务端 服务端需要接收客户端发来的描述，服务端会将其删除相对应的世界的对应维度的联合域名描述文件
权限等级：2 (管理员)
副字幕
原来的字幕（域名名称）为标题
副字幕为副标题
副字幕是位于原字幕正下方的位置的被有相应权限的玩家添加的一段话，用于帮助玩家了解域名
副字幕的字号比原字幕小一号且每行中心位于中轴线上 如果过长应该主动分行 
副字幕的文本可以用/n换行 如果文本有/n那么不自动换行
给域名文件添加subtitle字段和subtitlecolor字段
给个人配置文件添加subtitlesize选项
可选项与字幕相同
没有字段则不显示副字幕
下文的流程相似指交互方式相同环节相似，代码可复用程度高，须参考
子指令 : `/areahint addsubtitle`
1.  名称：`addsubtitle`
2.  功能：与easyadd指令流程类似（参考easyadd指令）玩家的权限需要为管理员，或者被该域名所引用（与easyadd相同） 去除命名域名和联合域名的环节 但修改的是副标题的相关字段 
目标选择器：作用于玩家自己
权限等级：0 (使用该玩家的权限)
子指令 : `/areahint deletesubtitle`
1.  名称：`deletesubtitle`
2.  功能：与delete指令流程类似（参考delete指令）玩家的权限需要为管理员，或者被该域名所引用（与delete相同） 但修改的只是副标题的相关字段
目标选择器：作用于玩家自己
权限等级：0 (使用该玩家的权限)
子指令 : `/areahint replacesubtitlecolor`
1.  名称：`replacesubtitlecolor`
2.  功能：副字幕与字幕的颜色独立可显示颜色种类相同 该指令与replacecolor指令流程类似（参考replacecolor指令）玩家的权限需要为管理员，或者被该域名所引用（与easyadd相同） 去除命名域名和联合域名的环节 但修改的是副标题的相关字段
目标选择器：作用于玩家自己
权限等级：0 (使用该玩家的权限)
子指令 : `/areahint replacesubtitlesize`
1.  名称：`replacesubtitlesize`
2.  功能：与replacetitlesize指令流程类似（参考replacecolor指令）除了replacetitlesize的大小按钮外增加一个自动按钮 使用自动按钮时 副标题的大小比原字幕小一级（使用replacetitlesize时如果为自动需要自动更改） 但修改的是配置文件的副标题的相关字段
目标选择器：作用于玩家自己
权限等级：0

指令可视化       
目前所有的指令都是可以正常进行的 但是只局限于对话栏 这个功能在原指令的基础上将其进一步可视化
玩家（客户端）长按绑定的按键时（绑定键）时打开指令面板
指令面板有三个按钮 boundviz按键 命令面板按钮 language按键
玩家按下boundviz按键后就使用boundviz的指令
玩家按下命令面板按钮后进入命令面板 命令面板中有着所有指令的按钮按钮旁边有着该按钮的说明（相当于help指令）