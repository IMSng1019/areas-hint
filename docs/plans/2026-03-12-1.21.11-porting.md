# Areas Hint 1.21.11 Porting Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Port `areas-hint` from Fabric Minecraft 1.21.9 to 1.21.11 with minimal compatibility-only changes and no intentional behavior changes.

**Architecture:** Upgrade version metadata first, then use `./gradlew build` as the primary evidence source. Triage and fix one compiler/runtime compatibility error at a time, prioritizing `network/`, then `render/`, then `mixin/`, then `command/`, while maintaining a living todo list.

**Tech Stack:** Java 21, Fabric Loom, Fabric Loader, Fabric API, Yarn mappings, Sponge Mixin

---

## Working Rules

- Keep functionality unchanged.
- Make the smallest viable compatibility patch.
- Do not suppress errors by commenting code out.
- Add new build errors to the todo list as they appear.
- Re-run `./gradlew build` after each module pass and after each meaningful fix cluster.

## Current Version Baseline

- `minecraft_version=1.21.11`
- `yarn_mappings=1.21.11+build.4`
- `loader_version=0.18.4`
- `loom_version=1.14.9`
- `fabric_version=0.141.3+1.21.11`

## Porting Todo

- [x] Update version metadata in `gradle.properties`.
- [x] Update `fabric.mod.json` Minecraft and loader constraints.
- [ ] Run `./gradlew build` and capture the first full error set.
- [ ] Triage errors into `network`, `render`, `mixin`, `command`, and `other`.
- [ ] Fix `network` compatibility errors only.
- [ ] Rebuild and append any new errors to this list.
- [ ] Fix `render` compatibility errors only.
- [ ] Rebuild and append any new errors to this list.
- [ ] Fix `mixin` compatibility errors only.
- [ ] Rebuild and append any new errors to this list.
- [ ] Fix `command` compatibility errors only.
- [ ] Rebuild and verify no remaining compatibility regressions.

## Error Log

- Pending first build after version upgrade.
